{% extends 'base.html.twig' %}

{% block title %}RelationUtilisateur index{% endblock %}

{% block body %}
    <h1>RelationUtilisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>EstAccepte</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for relation_utilisateur in relation_utilisateurs %}
            <tr>
                <td>{{ relation_utilisateur.id }}</td>
                <td>{{ relation_utilisateur.estAccepte ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_relation_utilisateur_show', {'id': relation_utilisateur.id}) }}">show</a>
                    <a href="{{ path('app_relation_utilisateur_edit', {'id': relation_utilisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_relation_utilisateur_new') }}">Create new</a>
{% endblock %}
