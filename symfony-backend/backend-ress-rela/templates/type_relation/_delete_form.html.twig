<form method="post" action="{{ path('app_type_relation_delete', {'id': type_relation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_relation.id) }}">
    <button class="btn">Delete</button>
</form>
