{% extends 'base.html.twig' %}

{% block title %}TypeRessource index{% endblock %}

{% block body %}
    <h1>TypeRessource index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_ressource in type_ressources %}
            <tr>
                <td>{{ type_ressource.id }}</td>
                <td>{{ type_ressource.nom }}</td>
                <td>
                    <a href="{{ path('app_type_ressource_show', {'id': type_ressource.id}) }}">show</a>
                    <a href="{{ path('app_type_ressource_edit', {'id': type_ressource.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_ressource_new') }}">Create new</a>
{% endblock %}
